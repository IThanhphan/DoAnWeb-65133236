server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: fastfood
  datasource:
    url: "jdbc:mysql://localhost:3306/fast_food_management"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
